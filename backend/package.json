{
  "name": "wayflower-paper-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub account verification and paper publishing API for wayflower.github.io",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "multer": "^2.2.0"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  }
}
